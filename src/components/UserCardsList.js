import { Component } from 'react'
import { connect } from 'react-redux'

class UserCardsList extends Component {
  render() {
    return (
      <div>

      </div>
    )
  }
}

export default connect()(UserCardsList)
